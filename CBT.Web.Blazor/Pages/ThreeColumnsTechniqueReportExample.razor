@page "/diary/three-columns/report/example"
@inherits ThreeColumnsTechniqueReport
@{
    base.BuildRenderTree(__builder);
}
@code {
    protected override Task<string?> GetCurrentUserId()
    {
        return Task.FromResult<string?>(null);
    }

    protected override bool ReadOnly => true;
}
